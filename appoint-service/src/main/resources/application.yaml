spring:
  application:
    name: appoint-service
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3307}/${DATABASE:trail01}
    username: ${DB_USER:root}
    password: ${DB_PASSWORD:root}

  jpa:
    show_sql: true

server:
  port: 8087